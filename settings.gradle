rootProject.name = 'bfstats'

// include all projects from subprojects directory and name it without subproject/ prefix
file('subprojects').eachDir { dir ->
  include dir.name
  project(":${dir.name}").projectDir = dir
}

include 'jooq_gen'
