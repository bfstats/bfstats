pippo.mode=prod
application.languages=en

# Control the network ip address Pippo binds to
# Specify 0.0.0.0 for all available interfaces
server.host=0.0.0.0

# Specify the context path of the application
server.contextPath = /stats

server.port=8081
